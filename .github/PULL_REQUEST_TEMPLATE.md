## Summary

## Related issue

Closes #

## Changes

## Testing

- [] `cargo test --workspace` passes
- [] `cargo clippy --workspace --D warnings` passes with zero warnings
- [] New functions have at least one test
- [] New public contract functions have a `///` doc comment

## Storage

- [] If this PR writes to persistent storage, I have read `docs/EPOCH_MODEL.md` §3 and called `extend_ttl` appropriately.
- [] If this PR changes storage keys, I have updated the storage layout table in `docs/architecture.md`.

## Changelog

- [] Added a `CHANGELOG.md` entry under `[Unreleased]` (required for contract behavior, SDK, or public API changes; N/A otherwise)
