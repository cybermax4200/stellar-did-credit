[package]
name = "governance"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }
credit-oracle-types = { path = "../credit-oracle-types" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
credit-oracle = { path = "../credit-oracle" }
