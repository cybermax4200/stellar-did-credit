{
  "name": "stellar-did-credit",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "test": "cargo test --workspace && pnpm -r test",
    "lint": "cargo clippy --workspace -- -D warnings && pnpm -r lint",
    "build": "cargo build --release && pnpm -r build"
  }
}
